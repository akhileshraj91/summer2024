version https://git-lfs.github.com/spec/v1
oid sha256:8f3d3a63c7c0466e0c78165f5d606ee367cd5c63eba5f95fa1cafa8ee0a8b375
size 91

# Summer 2024: CQL-based Reinforcement Learning for Power/Performance Control

This repository contains code and resources for data generation, training, and reproduction of reinforcement learning (RL) based control using the Conservative Q-Learning (CQL) algorithm for power and performance management in computing systems.

## Main Components

### 1. Data Generation (`main_codes/data_generation.py`)
This script automates the process of collecting experimental data from various benchmark applications. It runs the applications under different power capping (PCAP) settings, collects power, energy, progress, and hardware performance counter (PAPI) data, and stores the results in CSV files. The data is then compressed for efficient storage and later use in RL training.

- **Usage:**
  ```bash
  cd main_codes
  python data_generation.py
  ```
  *Make sure to run this script from within the `main_codes` directory.*
- **Purpose:** Generates the dataset required for RL training by running benchmarks and recording system metrics.

### 2. RL Training (`main_codes/CQL_renewed.ipynb`)
This Jupyter notebook implements the Conservative Q-Learning (CQL) algorithm for offline RL training. It loads the data generated by `data_generation.py`, processes and normalizes it, and trains a Q-function to learn optimal power management policies. The notebook also provides utilities for data analysis and visualization.

- **Usage:**
  Open and run the notebook in Jupyter from the `main_codes` directory:
  ```bash
  cd main_codes
  jupyter notebook CQL_renewed.ipynb
  ```
  *Make sure your working directory is `main_codes` when running the notebook.*
- **Purpose:** Trains the RL agent using the collected dataset and evaluates its performance.

### 3. RL Control & Reproduction (`main_codes/RL_CQL_control.py`)
This script deploys the trained RL policy to control the power cap of the system in real time. It loads the trained model, monitors system metrics, and applies the learned policy to adjust the power cap dynamically during benchmark execution. The script also logs the results for further analysis and reproduction of experimental results.

- **Usage:**
  ```bash
  cd main_codes
  python RL_CQL_control.py --policy <trained_policy_file>
  ```
  *Make sure to run this script from within the `main_codes` directory.*
- **Purpose:** Reproduces the RL-based control experiments using the trained CQL policy.

## Reproducing the Pipeline
1. **Generate Data:** Change to the `main_codes` directory and run `data_generation.py` to collect training data.
2. **Train RL Agent:** In the `main_codes` directory, use `CQL_renewed.ipynb` to train the CQL agent on the collected data.
3. **Deploy & Reproduce:** In the `main_codes` directory, run `RL_CQL_control.py` with the trained policy to reproduce the RL-based control experiments.

## Directory Structure
- `main_codes/` — Main scripts and notebooks for data generation, training, and control.
- `experiment_data/` — Generated datasets and experiment results.
- `results/` — Output and logs from experiments.

## Requirements
- Python 3.x
- PyTorch
- NumPy, pandas, matplotlib
- nrm (Node Resource Manager)
- Jupyter Notebook (for `.ipynb` files)

Install dependencies with:
```bash
pip install -r requirements.txt
```

## Citation
If you use this code or data, please cite the corresponding paper or contact the authors for more information.


